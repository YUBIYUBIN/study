# Unit Test

# 테스트 3단계

## 1) 테스트 파일 만들기

- 보통 구현 코드가 `sum.js`라면, 테스트 파일은 `sum.test.js` 같은 이름으로 만든다.
    
    ```
    project/
     ├─ sum.js       ← 실제 구현
     └─ sum.test.js  ← 테스트 코드
    ```
    

## 2) 테스트 코드 작성

- `test` 함수로 개별 테스트 케이스 작성한다.
- 기본 구조
    
    ```jsx
    test("설명", () => {
      expect(실제값).매처(기대값);
    });
    ```
    
    <aside>
    💡
    
     **expect는 항상** 검사할 값을 감싸고, **matcher는 상황에 맞게 선택해서 붙인다!**
    
    ex)
    
    - 숫자 비교 → `toBe`, `toBeGreaterThan`
    - 객체 비교 → `toEqual` 또는 `toStrictEqual`
    - 배열 포함 여부 → `toContain`
    - 문자열 검증 → `toMatch`
    - 에러 테스트 → `toThrow`
    </aside>
    
- ex)
    
    ```jsx
    const sum = require('./sum');
    test('1 + 2 = 3', () => {
    expect(sum(1,2)).toBe(3);
    });
    test('-1 + 1 = 0', () => {
    expect(sum(-1,1)).toBe(0);
    });
    ```
    

### 자주 사용하는 matcher 정리

### **1) toBe**

- 값을 **엄격하게 === 비교**할 때 원시값(숫자, 문자열, boolean 등)에 사용한다.

```jsx
expect(sum(1, 2)).toBe(3);
```

---

### **2) toEqual**

- 객체/배열 등 참조 타입을 **구조적으로 비교한다.**

```jsx
expect({ a: 1 }).toEqual({ a: 1 });
```

---

### **3) toBeNull / toBeUndefined / toBeDefined**

- 특정 상태인지 검사한다!

```jsx
expect(value).toBeNull();
expect(value).toBeUndefined();
expect(value).toBeDefined();
```

---

### 4**) toBeTruthy / toBeFalsy**

- true인지 false인지 검사한다.

```jsx
expect(1).toBeTruthy();
expect(0).toBeFalsy();
```

---

### **5) toContain**

- 배열이나 문자열 안에 값이 포함되어 있는지 확인한다.

```jsx
expect([1,2,3]).toContain(2);
expect("hello world").toContain("world");
```

---

### **6) toHaveLength**

- 길이를 확인한다.

```jsx
expect([1,2,3]).toHaveLength(3);
```

---

### **7) toMatch**

- 문자열 또는 정규표현식 매칭

```jsx
expect("abcd").toMatch(/bc/);
```

---

### **8) toBeGreaterThan / toBeLessThan**

- 숫자 비교

```jsx
expect(10).toBeGreaterThan(5);
```

---

### **9) toThrow**

- 에러 발생 확인

```jsx
expect(() => {
  throw new Error("fail");
}).toThrow("fail");
```

```jsx
// sum.test.js
const sum = require('./sum');

test('1 + 2 = 3', () => {
  expect(sum(1, 2)).toBe(3);  // sum(1,2)가 3이 맞는지 체크
});

test('-1 + 1 = 0', () => {
  expect(sum(-1, 1)).toBe(0);
});
```

## 3) 테스트 실행

```bash
npm test
```

- Jest가 모든 `.test.js` 파일을 찾아서 실행한다.
    
    ⇒ 성공/실패 결과를 보여준다!
    

---

## 

# Jest 설치 방법

```bash
npm install --save-dev jest
```

- package.json 안에 `"test"` 스크립트 설정

```json
"scripts": {
  "test": "jest"
}
```

<aside>
💡

package.json 안에는 **프로젝트에서 자주 쓰는 명령어**를 등록할 수 있는 `"scripts"`라는 항목이 있다!

ex)

```json
{
  "scripts": {
    "start": "node index.js",
    //npm start 입력 시 node index.js 실행
    
    "test": "jest"
    //npm test 입력 시 자동으로 "test" 스크립트를 찾아 jest 실행
    
  }
```

</aside>

---

### 

---

Jest에서 테스트는 가장 기본적으로 이렇게 구성돼:

1. **test()** 또는 **it()** – 테스트의 제목과 실행할 콜백
2. **expect()** – 검사하려는 값을 감싸는 함수
3. **matcher(매처)** – 기대하는 조건을 정의하는 함수

즉, 구조는 항상:

```jsx
expect(실제값).매처(기대값)

```

expect는 “이 값을 확인해!” 라고 지정하는 역할이고,

매처는 “이렇게 되면 통과야!”라는 조건을 붙이는 역할이야.

그래서 **expect는 반드시 쓰고**,

그 뒤에 **상황에 맞는 매처를 붙이는 방식이 맞아.**

---

# ✅ 2. 가장 많이 쓰는 매처 종합 정리

Jest 공식 문서: [https://jestjs.io/docs/expect](https://jestjs.io/docs/expect)

아래 목록을 보면 전체 흐름이 바로 잡힐 거야.

---

# ✅ 3. expect + matcher의 기본 패턴 정리

✔ **expect는 항상** 검사할 값을 감싼다

✔ **matcher는 상황에 맞게 선택해서 붙인다**

예)

- 숫자 비교 → `toBe`, `toBeGreaterThan`
- 객체 비교 → `toEqual` 또는 `toStrictEqual`
- 배열 포함 여부 → `toContain`
- 문자열 검증 → `toMatch`
- 에러 테스트 → `toThrow`

이런 식으로 “조건”에 따라 매처를 고르는 거지.

---

# 🔥 핵심 요약

- **expect는 필수**
- **매처는 상황에 따라 선택**
- 테스트 구조는 언제나:

```jsx
test("설명", () => {
  expect(실제값).매처(기대값);
});

```

이 틀만 이해하면 모든 Jest 테스트가 다 읽혀.